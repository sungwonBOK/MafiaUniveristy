# 🎓 MafiaUniversity — 게임 개발 로드맵 (GAME_ROADMAP)

> 최종 업데이트: 2026-03-10 (투표 흐름 버그 기록 + 개발 철학 추가)  
> 이 문서는 전체 개발 할 일(To-Do), 역할 기획 확정본, 구현 단계를 통합 관리합니다.  
> `AI_HANDOVER.md`의 다음 단계 항목은 이 문서를 최우선 참고 자료로 사용하십시오.

---

## 🏛️ 0-A. 핵심 개발 철학 (가장 먼저 읽어야 하는 원칙)

> **코드를 작성할 때 아래 두 가지를 항상 동시에 고려해야 합니다.**

### 원칙 A — 안정성 우선
- **게임의 안정성은 어떤 신규 기능보다도 우선순위가 높다.**
- 새 기능을 추가하기 전에, 이미 구현된 흐름이 여러 세션에 걸쳐 안정적으로 동작하는지 먼저 검증한다.
- **소켓 이벤트** 리스너는 등록(`on`) 시점과 해제(`off`) 시점의 스코프를 항상 일치시킨다. (좀비 리스너 방지)
- **Phaser 씬 전환** 시 `shutdown()`에서 반드시 소켓 리스너와 Zustand 구독을 해제한다.
- 비동기 큐로 지연되는 작업이 있다면, 그 진입점에서 이전 상태를 먼저 정리(선제 호출)해 후발 이벤트를 차단한다.

### 원칙 B — 코드 비대화 금지
- **한 가지 문제를 해결할 때 코드는 가능한 한 최소한으로 추가되어야 한다.**
- 새 코드를 작성하기 전, 기존 구조(기존 함수, 기존 이벤트)로 해결 가능한지 먼저 검토한다.
- 근본 원인 파악에 시간을 투자하고, 원인 모를 방어 코드를 무분별하게 쌓는 방식은 지양한다.
- 각 클래스/함수는 **단일 책임 원칙(SRP)**을 지켜, 모든 것을 처리하는 '하나의 거대한 파일'이 되지 않도록 한다.

---


## ⚠️ 0. 역할/스킬 시스템 아키텍처 원칙 (반드시 준수)

> **역할과 스킬은 언제든지 추가·수정·제거될 수 있습니다.**  
> 아래 원칙을 어기면 역할이 1개 바뀔 때 코드 전체를 수정해야 하는 상황이 발생합니다.

### 원칙 1 — 역할 정의는 단 한 곳에서 (Single Source of Truth)
- 모든 역할 이름(`Role` 타입)과 진영(`faction`) 정보는 `packages/shared/src/index.ts` 하나에만 정의합니다.
- 서버·클라이언트 양쪽에서 이 파일을 import해서 사용. **절대 하드코딩 금지.**

### 원칙 2 — 스킬 로직은 역할별 전략 패턴으로 분리
- `GameHandler.ts`에서 `if (role === 'professor')` 같은 조건 분기를 **길게 나열하지 말 것.**
- 역할별 능력 처리는 별도 핸들러 또는 `AbilityService`(예정)에서 **role → handler 맵핑** 방식으로 관리.
  ```ts
  // 예시 구조 (직접 구현 시 참고)
  const abilityHandlers: Record<Role, AbilityHandler> = {
    professor: professorAbilityHandler,
    investigator: investigatorAbilityHandler,
    // 새 역할 추가 시 이 맵에만 추가하면 끝
  };
  ```

### 원칙 3 — 클라이언트 스킬 UI도 맵핑 구조로
- `UIScene.ts`에서 역할마다 쿨다운/버튼을 핸드코딩하지 말고, `ROLE_CONFIG` 같은 설정 객체에서 쿨다운·아이콘·설명을 읽어 렌더링.
  ```ts
  // 예시
  const ROLE_CONFIG: Record<Role, RoleConfig> = {
    professor:    { cooldown: 30000, icon: '📋', label: 'F학점 부여' },
    investigator: { cooldown: 45000, icon: '🔍', label: '신원 조회'  },
    topstudent:   { cooldown: 40000, icon: '💀', label: '시체 분석'  },
    // ...
  };
  ```

### 원칙 4 — 서버 PlayerState에 '표시 정보'와 '실제 정보' 분리 ⚠️
- **출석 대리인(impersonator) 변신 스킬** 때문에 반드시 필요한 구조입니다.
- `Player` 클래스에는:
  - `realNickname` / `realColor` — 실제 정체 (서버 내부 전용, 절대 클라이언트 전체에 브로드캐스트 X)
  - `displayNickname` / `displayColor` — 현재 표시 이름과 색상 (클라이언트 렌더링용)
- 변신 중에는 `displayNickname`, `displayColor`만 변경 → 클라이언트는 이 값으로만 렌더링.
- **조사(홍보국장)·시체분석(과탑) 스킬 결과는 반드시 `realNickname` 기준으로 반환** (변신 중인 출석대리인을 속일 수 없게).

### 원칙 5 — 역할 추가 시 체크리스트
새 역할을 추가할 때 **반드시 아래 파일 모두** 수정:
1. `packages/shared/src/index.ts` — `Role` 타입 유니온에 추가
2. `packages/server/src/services/GameService.ts` — `buildRolePool()`에 역할 배분 조건 추가
3. `packages/server/src/services/AbilityService.ts` (예정) — 능력 핸들러 맵에 추가
4. `packages/client/src/game/scenes/UIScene.ts` — `ROLE_CONFIG`에 쿨다운·아이콘 추가
5. `AI_HANDOVER.md` + `GAME_ROADMAP.md` — 역할 테이블 업데이트

---

## 📌 1. 기술 부채 & 미완성 항목

### 🔴 우선순위 높음 — 핵심 기능 결함

| # | 파일 | 문제 | 비고 |
|---|------|------|------|
| 1 | `GameHandler.ts` L.109 | 탐정/의사 능력 로직 서버 미구현 (`// TODO` 명시) | 역할 시스템 재설계 시 함께 처리 |
| 2 | `EndScene.ts` + 서버 | 게임 종료 후 `Room.phase`가 `'ended'`에 갇힘 → 재시작 불가 | `RESET_GAME` 이벤트 추가 필요 |
| 3 | `GameScene.ts` | 게임 도중 disconnect/reconnect 처리 없음 | `PLAYER_LEFT` 이벤트 필요 |
| 4 | `GameScene.ts` | 역할 구분 없이 Space키가 항상 '킬' 로직 작동 | 역할별 능력 분기 추가 필요 |

### 🟡 우선순위 중간 — UX/게임 흐름

| # | 파일 | 문제 |
|---|------|------|
| 5 | `PreloadScene.ts` | 실제 타일맵 로딩 코드 주석처리 (맵 파일 없음) |
| 6 | `PlayerEntity.ts` | `die()`가 단순 반투명 처리만 — 사체 스프라이트/킬 이펙트 없음 |
| 7 | `UIScene.ts` | 스킬 쿨다운 UI 없음 (25초 쿨다운이 화면에 표시 안됨) |
| 8 | `UIScene.ts` | 십자선(Crosshair) 타겟팅 UI 없음 |
| 9 | `GameHandler.ts` + `VotingModal.tsx` | **투표/스킵 완료 후 다음 단계(게임 재개)로 넘어가지 않음** — 모든 생존자가 투표를 마쳐도 게임이 멈춰있는 현상. 서버 측 투표 집계 완료 후 `VOTE_RESULT` 전송은 되지만 클라이언트 VotingModal이 닫히지 않거나 게임 재개 처리가 없음 |
| 10 | `VotingModal.tsx` | 투표 제한시간 없음 (무제한 대기 — 한 명이 투표 안 하면 영구 대기 상태) |
| 11 | `VotingModal.tsx` | 실시간 투표 현황 표시 없음 (누가 이미 투표했는지 모름) |
| 12 | `WaitingRoom.tsx` | 게임 종료 후 복귀 시 `isReady` 상태 초기화 안됨 |

### 🟢 우선순위 낮음 — 구조/안정성

| # | 파일 | 문제 |
|---|------|------|
| 13 | `shared/index.ts` | `RESET_GAME`, `PLAYER_LEFT`, `INVESTIGATE_BODY` 등 이벤트 미등록 |
| 14 | `GameScene.ts` | `shutdown()`에서 `VOTE_RESULT` 이벤트 off 누락 |
| 15 | `useGameStore.ts` | 게임 종료 후 `remotePlayers` Map 초기화 안됨 (재플레이 시 잔여 데이터) |

---

## 🎭 2. 역할 시스템 기획 확정본

> 게임명 변경 방향: **[졸업 전쟁 (Graduation War)]**

### 팀 구성

| 진영 | 목표 |
|------|------|
| 🎓 **Team 졸업** (시민 진영) | 모든 교수진을 투표로 퇴출하거나 모든 과제(미션) 완료 |
| 📚 **Team 유급** (마피아 진영) | 학생 수 ≤ 교수진 수가 되도록 학생 제거 |
| 🏖️ **Team 휴학** (중립 진영) | 고유 승리 조건 달성 (게임 즉시 종료) |

---

### 🎓 Team 졸업 — 시민 진영 (학생)

#### 학생회장 (리더 / 구 `citizen` 확장)
- **[스킬] 캠퍼스 비상방송**: 모든 플레이어의 현재 위치를 맵에 3초간 노출 (게임당 1회)
- 쿨다운: 1회성
- 코드 Role: `president`

#### 홍보국장 (조사관 / 구 `detective` 대체)
- **[스킬] 신원 조회**: 지목한 플레이어가 '학생'인지 '교수진'인지 확인 (본인만 결과 확인 가능)
- 쿨다운: 45초
- 코드 Role: `investigator`

#### 새내기 (탱커)
- **[패시브] 과잠 입기**: 교수의 킬 공격을 1회 방어하는 보호막 소지 (1번만)
- **[액티브] 패기**: 스킬 사용 시 5초간 무적 상태 (쿨다운: 60초)
- 코드 Role: `freshman`

#### 취준생 (미션 특화)
- **[패시브] 스펙 쌓기**: 모든 과제(미션) 수행 속도 2배
- 미션 시스템 구현 이후 활성화
- 코드 Role: `jobseeker`

#### 과탑 (추적자)
- **[액티브] 시체 분석**: 사망한 플레이어의 시체에 다가가 능력 사용 시, 해당 플레이어를 죽인 킬러의 **현재 위치 방향을 화살표로 5초간 표시** (본인에게만 보임)
- 쿨다운: 40초
- 코드 Role: `topstudent`
- 구현 메모: 시체 엔티티에 `killerId` 메타데이터 저장. 서버가 킬러 현재 위치를 요청자에게만 전송. 클라이언트는 Phaser `Graphics`로 방향 화살표 렌더링 후 5초 뒤 자동 삭제.

---

### 📚 Team 유급 — 마피아 진영 (교수진)

#### 교수 (메인 킬러 / 구 `mafia` 대체)
- **[스킬] F학점 부여**: 근접한 학생 1명 제거 (쿨다운: 30초)
- **[스킬] 사보타주 실행** (2단계):
  - 시험 기간: 일정 범위 시야 차단
  - 수강신청 서버 다운: 미션 진행 불가
- 코드 Role: `professor`

#### 대학원생 (서포터)
- **[스킬] 연구실 청소**: 시체를 즉시 소멸 (쿨다운: 50초)
- **[이동] 연구실 통로**: 맵 특정 환풍구 포인트 간 순간이동 (2단계)
- 코드 Role: `grad_student`

#### 출석 대리인 (변신수) ⚠️ 아키텍처 주의
- **[스킬] 출석 조작**: 일정 시간 동안 타 플레이어의 외형·닉네임으로 변신 (쿨다운: 90초)
- 구현 난이도 높음 — 3단계에서 구현
- 코드 Role: `impersonator`
- **🚨 아키텍처 필수 조건** (설계 초기부터 반영해야 함):
  - 서버 `Player` 클래스에 `realNickname`/`realColor`(실제값) + `displayNickname`/`displayColor`(표시값) 분리 필수
  - 클라이언트는 항상 `displayNickname`, `displayColor`로만 렌더링
  - 소켓으로 전송하는 `PlayerState`에도 `displayNickname`, `displayColor` 필드 포함 (스펙 확정 전부터 필드 예약)
  - 조사 스킬(홍보국장), 시체분석(과탑) 등 역할 파악 스킬은 반드시 `real*` 값 기준으로 결과 반환
  - 변신 종료 시 서버가 `display*` 값을 `real*` 값으로 자동 복원 후 전체 브로드캐스트

---

### 🏖️ Team 휴학 — 중립 진영

#### CC (캠퍼스 커플 — 2인 1조)
- **[조건] 공동 생존 승리**: 게임 종료 시까지 커플 두 명 모두 생존 시 즉시 단독 승리
- **[특징] 연결**: 한 명 사망 시 나머지도 즉시 사망 (자퇴)
- 랜덤 커플 매칭: 게임 시작 시 서버가 무작위 2명을 커플로 지정
- 코드 Role: `couple`

#### 아싸 (솔로 생존자)
- **[조건] 0표 생존 승리**: 5번의 투표 라운드 동안 한 번도 투표 받지 않으면 즉시 단독 승리
- 코드 Role: `loner`

---

### 스킬 쿨다운 요약

| 역할 | 스킬 | 쿨다운 |
|------|------|--------|
| 교수 | F학점 부여 | 30초 |
| 홍보국장 | 신원 조회 | 45초 |
| 과탑 | 시체 분석 | 40초 |
| 새내기 | 패기(무적) | 60초 |
| 학생회장 | 비상방송 | 1회성 |
| 대학원생 | 연구실 청소 | 50초 |
| 출석 대리인 | 출석 조작 | 90초 |

---

## 🗺️ 3. 사보타주 & 미션 시스템 (2단계)

### 사보타주 종류
- **수강신청 서버 마비**: 해결 전까지 모든 학생 미션 진행 불가
- **에타(Everytime) 서버 점검**: 채팅창 제한 또는 모든 닉네임 '익명' 표시
- **전공 서적 품절**: 특정 장소 이동 후 책 보충 긴급 미션 발생

### 미션(과제) 예시
- **학식 먹기**: 학생 식당에서 식권 발권 미니게임
- **레포트 제출**: 학과 사무실 메일함에 파일 넣기
- **출석 체크**: 지정된 강의실 내 QR코드 찍기 (미니게임)
- **전공책 챙기기**: 사물함 비밀번호 맞추기

---

## 🏗️ 4. 구현 단계별 로드맵

### 1단계 — 안정성 + 역할 기반 구조 구축
> 기존 코드의 결함을 수정하고, 새 역할 타입을 코드에 반영합니다.

- [x] `EVENTS` 상수에 `RESET_GAME`, `GAME_RESET` 추가 (shared) ✅
- [ ] `EVENTS` 상수에 `PLAYER_LEFT`, `INVESTIGATE_BODY` 추가 (shared)
- [x] `Role` 타입 10종으로 확장 — 향후 진행 (shared 구조 준비됨)
- [x] `Player.reset()` 메서드 추가 (server) ✅
- [x] `GameHandler` — 게임 종료 후 방 리셋 핸들러(`RESET_GAME`) 추가 ✅
- [x] `EndScene` — 방장/일반 플레이어 분기, `GAME_RESET` 이벤트 수신 후 대기방 복귀 ✅
- [x] `useGameStore` — `resetGameState()` 추가, 재플레이 시 `remotePlayers`/역할/회의/투표채팅 일괄 초기화 ✅
- [x] `GameScene.shutdown()` — `VOTE_RESULT` off 누락 수정 ✅
- [ ] `EVENTS` 상수에 `PLAYER_LEFT`, `INVESTIGATE_BODY` 추가 (shared) — 2단계 스킬 구현 시 함께 처리
- [ ] `Role` 타입 10종으로 확장 — 2단계 진입 시 처리
- [ ] `GameService.assignRoles()` — 새 역할 배분 로직으로 교체 — 2단계 진입 시 처리
- [ ] `WaitingRoom` — 복귀 시 Ready 상태 서버 동기화 확인 (현재 `GAME_RESET` → `setCurrentRoom`으로 자동 처리됨, 추가 이슈 발견 시 수정)

### 2단계 — 핵심 스킬 구현 (1단계 완료 후)
> 구현 가능한 역할부터 스킬 로직을 붙입니다.

- [ ] **교수**: 기존 마피아 킬 로직 → `professor`로 리네이밍 + 쿨다운 30초
- [ ] **홍보국장**: `INVESTIGATE_PLAYER` 이벤트 — 역할 노출 없이 진영(학생/교수)만 반환
- [ ] **과탑**: `INVESTIGATE_BODY` — 서버에서 킬러 위치 반환, 클라이언트 화살표 5초 렌더링
- [ ] **새내기**: 서버 Player에 `shield: boolean` 추가 → 킬 처리 시 방어 판정
- [ ] **아싸**: 투표 집계 시 0표 라운드 카운트 체크, 5회 달성 시 즉시 승리
- [ ] UIScene — 역할별 쿨다운 UI 추가
- [ ] UIScene — 타겟팅 십자선(Crosshair) UI 추가
- [ ] **투표 흐름 완성** — 투표/스킵 완료 후 VotingModal 닫힘 + 게임 재개 처리
  - 서버: `VOTE_RESULT` 수신 후 클라이언트가 Modal을 닫고 `playing` 상태로 복귀하는지 확인
  - 클라이언트: `VOTE_RESULT` 수신 시 `isMeeting = false` 처리가 VotingModal에 정확히 반영되는지 검증
- [ ] VotingModal — 투표 제한시간 (60초) 추가 — 시간 초과 시 자동 스킵 처리(서버 주도)
- [ ] VotingModal — 실시간 투표 현황 표시 (몇 명이 투표했는지 표시)
- [ ] PlayerEntity — 사체 스프라이트로 교체 (`die()` 개선)
- [ ] GameScene — 킬 이펙트 파티클

### 3단계 — 고급 스킬 + 맵 + 미션
> 복잡도 높은 기능을 구현합니다.

- [ ] **학생회장**: 전체 위치 브로드캐스트 → 3초간 맵에 마커 표시
- [ ] **대학원생**: 시체 소멸 스킬 (시체 엔티티 제거 + 서버 동기화)
- [ ] **CC**: 커플 링크 시스템 — 한 명 사망 시 서버가 나머지도 즉시 kill
- [ ] **출석 대리인**: 변신 시스템 — 닉네임·색상 임시 변경, 역할 유출 방지
- [ ] **취준생**: 미션 시스템 구축 후 속도 보정 패시브 연동
- [ ] 프로토타입 타일맵 제작 + `PreloadScene` 로딩 활성화
- [ ] 사보타주 시스템 (수강신청 서버 다운, 에타 점검 등)
- [ ] 교수: `대학원생` 환풍구 순간이동 포인트 맵에 배치

---

## 5. 결정된 세부 사항 메모

- 과탑 화살표: **과탑 본인에게만** 표시 (전체 공개 아님)
- 중립 진영 쿨다운 없음 — 조건 달성 시 자동 승리
- 중립 승리 시 → 교수·학생 진영 모두 패배 처리
- `skills.txt` 파일 → 이 문서로 통합 완료, 삭제 가능
- **역할/스킬은 언제든지 수정·추가·제거될 수 있으므로 항상 확장하기 쉬운 구조로 코드를 짤 것** (섹션 0 원칙 준수)
- **출석 대리인의 변신 스킬은 `displayNickname`/`displayColor` 분리 구조가 설계 초기부터 반영되어야 하며, 나중에 추가하면 대규모 리팩토링 발생**
